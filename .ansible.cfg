[defaults]
# CRITICAL FIX: Explicitly tell Ansible where to look for roles.
# The 'roles' directory is one level up from the current directory, 
# and it should also search the standard collection path.
roles_path = ./roles:~/.ansible/roles:/usr/share/ansible/roles