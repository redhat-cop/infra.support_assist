---
- name: Converge
  hosts: all
  tasks:
    - name: "Include aap_api_dump"
      ansible.builtin.include_role:
        name: "aap_api_dump"
      vars:
        aap_gateway_url: "https://example.com"
        aap_username: "test_user"
        aap_password: "test_password"
        aap_validate_certs: false
      # Note: This will fail without actual AAP instance, but tests role structure

