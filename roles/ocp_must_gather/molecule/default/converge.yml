---
- name: Converge
  hosts: all
  tasks:
    - name: "Include ocp_must_gather"
      ansible.builtin.include_role:
        name: "ocp_must_gather"
      vars:
        ocp_api_url: "https://api.example.com:6443"
        ocp_username: "test_user"
        ocp_password: "test_password"
        ocp_validate_certs: false
      # Note: This will fail without actual OpenShift cluster, but tests role structure
...
