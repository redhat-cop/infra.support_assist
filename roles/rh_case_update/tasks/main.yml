---
- name: Pre-Validation
  ansible.builtin.include_tasks: pre-val.yml

- name: Case Updates Loop
  loop: "{{ case_updates_needed }}"
  loop_control:
    label: "Updating case with update"
    loop_var: case_update
  ansible.builtin.include_tasks: case_updates_loop.yml
...
