---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Verify role execution attempted
      ansible.builtin.debug:
        msg: "Role execution completed (may have failed due to invalid token, which is expected in test environment)"

