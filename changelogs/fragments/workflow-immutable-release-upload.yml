---
bugfixes:
  - Harden GitHub release steps for immutable releases (skip redundant ``gh release edit``, create draft releases with ``--target`` the release branch, verify the version tag exists before upload, skip upload if the tarball asset is already attached).
...
